# Variáveis de Ambiente para Testes E2E
# Copie este arquivo para .env quando rodar testes

NODE_ENV=test

# Mock da API4AI (não consome quota)
API4AI_MOCK=true
API4AI_KEY=mock-key-for-tests

# Supabase (usar mesmas credenciais do .env de dev/prod)
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui
SUPABASE_JWT_SECRET=sua-jwt-secret-aqui

# Porta do servidor
PORT=3000
